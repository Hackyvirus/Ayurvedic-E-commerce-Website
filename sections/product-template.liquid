    <style>
        .product-title-2 {
            font-family: Lora;
            font-size: 40px;
            width: 100%;
            font-weight: 400;
            line-height: 1.3;
        }

        .product-box {
	width: 81dvw;
	margin: 50px auto 0;
	display: flex;
	justify-content: space-between;
	height: 500px;
}
        .left-product-box {
	width: 45%;
	display: flex;
	align-items: center;
	justify-content: center;
}
.right-product-box {
	width: 50%;
	height: 450px;
	display: flex;
	flex-direction: column;
	justify-content: space-around;
}
        .product-details {
            padding: 10px;
            display: flex;
            flex-direction: column;
            align-items: start;
            color: #000;
            height: 280px;
            justify-content: space-evenly;
            box-sizing: border-box;
        }

        .product-details p{
            font-size: 20px;
        }
        .container {
	position: relative;
	height: 450px;
	display: flex;
	flex-direction: column;
	justify-content: space-between;
}

        .mySlides {
            display: none;
        }

        .cursor {
            cursor: pointer;
        }

        .prev,
        .next {
            cursor: pointer;
            position: absolute;
            top: 40%;
            width: auto;
            padding: 16px;
            margin-top: -50px;
            color: white;
            font-weight: bold;
            font-size: 20px;
            border-radius: 0 3px 3px 0;
            user-select: none;
            -webkit-user-select: none;
        }
        .add-to {
            width: 85%;
            height: 230px;
            display: flex;
            flex-direction: column;
            justify-content: space-around;
        }

        .next {
            right: 0;
            border-radius: 3px 0 0 3px;
        }

        .prev:hover,
        .next:hover {
            background-color: rgba(0, 0, 0, 0.8);
        }

        .numbertext {
            color: #f2f2f2;
            font-size: 12px;
            padding: 8px 12px;
            position: absolute;
            top: 0;
        }

        .row{
            height: 120px;
        }

        .row:after {
            content: "";
            display: table;
            clear: both;
        }

        .column {
            float: left;
            width: 99px;
        }

        .demo {
            opacity: 0.6;
        }

        .active,
        .demo:hover {
            opacity: 1;
        }
        .quantity-input::-webkit-outer-spin-button,
        .quantity-input::-webkit-inner-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }

        .quantity-input[type=number] {
            -moz-appearance: textfield;
        }

        .quantity-control {
            width: 30%;
            height: 35px;
            display: flex;
            justify-content: space-around;
            border: 1px solid black;
            background: white;
        }
        .up-down {
            width: 25%;
            border: none;
            cursor: pointer;
            background: white;
            border: 0;
            font-size: 25px;
        }
        .up-down-box {
            width: 85%;
            display: flex;
            justify-content: start;
            align-items: center;
            gap: 20px;
        }

        .quantity-input {
            text-align: center;
            font-size: 20px;
            font-family: Lora;
            width: 25%;
            border: 0;
        }
        .buy-now-button{
            font-family: Montserrat;
            width: 45%;
            height: 50px;
            border: 1px solid black;
            border-radius: 5px;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 20px;
            background-color: #fff;
            color: #000;
            cursor: pointer;
            transition: background-color 0.3s ease, color 0.3s ease;
        }
        .buy-now-button:hover {
          background:  #2f7c41;;
          color: #fff;
          }
        .add-to-cart-button:hover {
          background:  #2f7c41;;
          color: #fff;
          }
        .buy-add-to-cart{
            width: 100%;
            display: flex;
            justify-content: space-around;
        }
        .add-to-cart-button {
            font-family: Montserrat;
            width: 45%;
            height: 50px;
            border: 1px solid black;
            border-radius: 5px;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 20px;
            background-color: #fff;
            color: #000;
            cursor: pointer;
            transition: background-color 0.3s ease, color 0.3s ease;
        }
        .added {
            background-color: green;
            color: #fff;
        }
        .add-buy{
            width: 100%;
            display: flex;
            justify-content: space-between;
        }
         /* Previous & next buttons */
    .prev, .next {
        cursor: pointer;
        position: absolute;
        top: 40%;
        width: auto;
        padding: 16px;
        margin-top: -50px;
        color: white;
        font-weight: bold;
        font-size: 20px;
        border-radius: 0 3px 3px 0;
        user-select: none;
        -webkit-user-select: none;
    }

    .prev:hover, .next:hover {
        background-color: rgba(0, 0, 0, 0.8);
    }

    /* Number text (1/3 etc) */
    .numbertext {
        color: #f2f2f2;
        font-size: 12px;
        padding: 8px 12px;
        position: absolute;
        top: 0;
    }

    /* Thumbnail image styling */
    .row {
        height: 120px;
    }

    .row:after {
        content: "";
        display: table;
        clear: both;
    }

    .column {
        float: left;
        width: 99px;
    }

    .demo {
        opacity: 0.6;
    }

    .active, .demo:hover {
        opacity: 1;
    }
    </style>

<div class="product-box">
  <div class="left-product-box">
    <div class="container">
        {% if product.images.size > 0 %}
            {% for image in product.images %}
                <div class="mySlides">
                    <div class="numbertext">{{ forloop.index }}</div>
                    <img width="" height="" class="demo cursor" src="{{ image.src | img_url: 'large' }}" style="width:100%" onclick="currentSlide({{ forloop.index }})" alt="Product Image {{ forloop.index }}">
                </div>
            {% endfor %}
            <a class="prev" onclick="plusSlides(-1)">&#10094;</a>
            <a class="next" onclick="plusSlides(1)">&#10095;</a>
        {% else %}
            <p>No images available for this product</p>
        {% endif %}
        
        <!-- Thumbnail images -->
        <div class="row">
            {% for image in product.images %}
                <div class="column">
                    <img width="" height="" class="demo cursor" src="{{ image.src | img_url: 'compact' }}" style="width:100%" onclick="currentSlide({{ forloop.index }})" alt="Product Image {{ forloop.index }}">
                </div>
            {% endfor %}
        </div>
    </div>
</div>
    <div class="right-product-box">
        <div class="product-details">
            <h2 class="product-title-2 ">{{ product.title }}</h2>
            <p class="description">{{ product.description }}</p>
            <p class="price">{{ product.price | money }}</p>
        </div>
        <form class="add-to" id="product-form-{{ product.id }}" action="/cart/add" method="post">
            <input type="hidden" name="id" value="{{ product.variants.first.id }}">
            <div class="up-down-box">
                <div class="quantity">Quantity: </div>
                <div class="quantity-control">
                    <button class="up-down" type="button" id="decrease-quantity-{{ product.id }}">-</button>
                    <input class="quantity-input" type="number" id="quantity-{{ product.id }}" name="quantity" value="1" min="1">
                    <button class="up-down" type="button" id="increase-quantity-{{ product.id }}">+</button>
                </div>
            </div>
            <div class="add-buy">
                <button type="button" class="add-to-cart-button" id="add-to-cart-button-{{ product.id }}">Add to Cart</button>
                <button type="button" class="buy-now-button" id="buy-now-button-{{ product.id }}">Buy Now</button>
            </div>
        </form>
    </div>
</div>

<script>
  
  var slideIndex = 1;

function plusSlides(n) {
    showSlides(slideIndex += n);
}

function currentSlide(n) {
    showSlides(slideIndex = n);
}

function showSlides(n) {
    var i;
    var slides = document.getElementsByClassName("mySlides");
    if (n > slides.length) { slideIndex = 1 }
    if (n < 1) { slideIndex = slides.length }
    for (i = 0; i < slides.length; i++) {
        slides[i].style.display = "none";
    }
    slides[slideIndex - 1].style.display = "block";
}

showSlides(slideIndex); // Show the initial slide when the page loads

    document.addEventListener('DOMContentLoaded', function() {
        var productId = {{ product.id }};
        var quantityInput = document.getElementById('quantity-' + productId);
        var increaseButton = document.getElementById('increase-quantity-' + productId);
        var decreaseButton = document.getElementById('decrease-quantity-' + productId);
        var addToCartButton = document.getElementById('add-to-cart-button-' + productId);
        var buyNowButton = document.getElementById('buy-now-button-' + productId);

        increaseButton.addEventListener('click', function() {
            quantityInput.value = parseInt(quantityInput.value) + 1;
        });

        decreaseButton.addEventListener('click', function() {
            if (quantityInput.value > 1) {
                quantityInput.value = parseInt(quantityInput.value) - 1;
            }
        });

        addToCartButton.addEventListener('click', function() {
            var form = new FormData();
            form.append('id', document.querySelector('#product-form-' + productId + ' input[name="id"]').value);
            form.append('quantity', quantityInput.value);

            fetch('/cart/add', {
                method: 'POST',
                body: form
            }).then(response => {
                if (response.ok) {
                    addToCartButton.classList.add('added');
                    addToCartButton.innerText = 'Added to Cart';
                } else {
                    alert('Failed to add product to cart.');
                }
            }).catch(error => {
                console.error('Error:', error);
            });
        });

        buyNowButton.addEventListener('click', function() {
            var form = new FormData();
            form.append('id', document.querySelector('#product-form-' + productId + ' input[name="id"]').value);
            form.append('quantity', quantityInput.value);

            fetch('/cart/add', {
                method: 'POST',
                body: form
            }).then(response => {
                if (response.ok) {
                    window.location.href = '/cart';
                } else {
                    alert('Failed to add product to cart.');
                }
            }).catch(error => {
                console.error('Error:', error);
            });
        });
    });

    
    var slideIndex = 1;
    showSlides(slideIndex);

    function plusSlides(n) {
        showSlides(slideIndex += n);
    }

    function currentSlide(n) {
        showSlides(slideIndex = n);
    }

    function showSlides(n) {
        var i;
        var slides = document.getElementsByClassName("mySlides");
        var dots = document.getElementsByClassName("demo");
        if (n > slides.length) { slideIndex = 1 }
        if (n < 1) { slideIndex = slides.length }
        for (i = 0; i < slides.length; i++) {
            slides[i].style.display = "none";
        }
        for (i = 0; i < dots.length; i++) {
            dots[i].className = dots[i].className.replace(" active", "");
        }
        slides[slideIndex - 1].style.display = "block";
        dots[slideIndex - 1].className += " active";
    }
</script>
